import styled from "@emotion/styled";

const StyledLine = styled.hr`
  border: 0;
  border-top: 1px solid #333;
  width: 100%;
  display: block;
`;
const Line = () => {
  return <StyledLine />;
};

export default Line;
